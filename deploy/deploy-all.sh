#!/usr/bin/env bash
# ---------------------------------------------------------------------------
# รันทุกขั้นตอน deploy ต่อกันในคำสั่งเดียว
#   ./deploy-all.sh
# (เหมาะกับการ deploy ครั้งแรก — ครั้งถัดไปถ้าแก้แค่โค้ด รันแค่ 01 + 03 พอ)
# ---------------------------------------------------------------------------
set -e
cd "$(dirname "$0")"

bash 00-check.sh
bash 01-build-push.sh
bash 02-infra.sh
bash 03-deploy.sh
